# optional values for spirng.jap.hibernate.ddl-auto 
#-------------------------------------------------------
# none : default, default for MySQL.  No change is made to thedatabase structure
# update: Hibernate changes the database acording to the given entity structures..
# create: Creates the dtabase every time but does not drop it on close
# create-drop: Creates the database and drops it when SessionFactory closes
spring.jpa.hibernate.ddl-auto=none
spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:3306/pugnator_rdb
spring.datasource.username=foobar
spring.datasource.password=f00b@r
spring.jpa.show-sql=true

#spring.jpa.properties.hibernate.format_sql=true